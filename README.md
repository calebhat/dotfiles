# my omarchy dotfiles

git pull in the home directory
command: 'stow --adopt nvim' for each folder #merges existing configs into dotfiles folder, have to do this temporarily to keep system from crashing. 
git reset --hard #reverts back to pulled versions of files
